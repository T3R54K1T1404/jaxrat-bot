[tool.poetry]
name = "jaxrat-bot"
version = "1.0.0"
description = "JaxRaT V5 Telegram Bot"

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
